export function DiagramNodeEditor() { return <div className="rounded-md bg-amber-50 p-3 text-xs leading-5 text-amber-900"><strong>Structured JSON only.</strong> Use top-level <code>groups</code>, <code>nodes</code>, and <code>edges</code> arrays. Node and edge IDs use lowercase slug format. HTML, SVG source, SQL, and executable content are rejected by the schema shape and rendered only as text.</div>; }
