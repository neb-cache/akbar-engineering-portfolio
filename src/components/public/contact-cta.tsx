import Link from "next/link";
import { ArrowRight, BriefcaseBusiness, CodeXml, Mail } from "lucide-react";
import type { PublicSiteProfile } from "@/lib/public/data";

const Github = CodeXml;
const Linkedin = BriefcaseBusiness;

export function ContactCta({ profile }: { profile: PublicSiteProfile }) { const socials=[profile.email&&{href:`mailto:${profile.email}`,label:"Email",icon:Mail},profile.linkedinUrl&&{href:profile.linkedinUrl,label:"LinkedIn",icon:Linkedin},profile.githubUrl&&{href:profile.githubUrl,label:"GitHub",icon:Github}].filter(Boolean) as Array<{href:string;label:string;icon:typeof Mail}>; return <section className="border-t border-[var(--border)] bg-[var(--accent-green)] py-20 sm:py-28"><div className="public-container grid gap-10 lg:grid-cols-[1fr_auto] lg:items-end"><div><p className="editorial-label !text-[var(--paper-soft)]">Open correspondence</p><h2 className="editorial-title mt-6 text-6xl sm:text-8xl">Building something complex?</h2><p className="mt-5 font-serif text-3xl text-[var(--paper-soft)]">Let’s discuss the system behind it.</p></div><div><Link href="/contact" className="focus-ring inline-flex items-center gap-3 bg-[var(--paper)] px-6 py-4 text-sm font-semibold text-[var(--ink)]">Contact me <ArrowRight size={17}/></Link>{socials.length>0&&<div className="mt-6 flex flex-wrap gap-4">{socials.map(({href,label,icon:Icon})=><a key={label} href={href} target={href.startsWith("http")?"_blank":undefined} rel={href.startsWith("http")?"noreferrer":undefined} className="focus-ring inline-flex items-center gap-2 text-xs text-[var(--paper-soft)] underline"><Icon size={14}/>{label}</a>)}</div>}</div></div></section>; }
