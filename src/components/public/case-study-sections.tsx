import type { PublicCaseStudySectionRow } from "@/types/database";
export function CaseStudySections({ sections }: { sections: PublicCaseStudySectionRow[] }) { if(!sections.length)return null; return <div className="space-y-14">{sections.map((section,index)=><section key={section.id} aria-labelledby={`section-${section.id}`} className="grid gap-5 sm:grid-cols-[3rem_1fr]"><span className="font-mono text-xs text-[var(--accent-gold)]">{String(index+1).padStart(2,"0")}</span><div><p className="editorial-label">{section.section_type}</p><h2 id={`section-${section.id}`} className="mt-3 font-serif text-4xl">{section.title}</h2><div className="mt-5 max-w-3xl whitespace-pre-line text-sm leading-8 text-[var(--text-secondary)]">{section.content}</div></div></section>)}</div>; }
