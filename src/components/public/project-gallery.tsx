import Image from "next/image";
import type { ProjectImageRow } from "@/types/database";

export function ProjectGallery({ images, cover, title }: { images: ProjectImageRow[]; cover: string | null; title: string }) { const records=cover?[{id:"cover",image_url:cover,alt_text:`${title} cover image`,caption:null,project_id:"",sort_order:-1,created_at:""},...images.filter(image=>image.image_url!==cover)]:images; if(!records.length)return null; return <section aria-labelledby="project-gallery"><h2 id="project-gallery" className="editorial-label mb-5">Project gallery</h2><div className="grid gap-4 md:grid-cols-2">{records.map((image,index)=><figure key={image.id} className={index===0?"md:col-span-2":""}><div className="relative aspect-[16/9] overflow-hidden border border-[var(--border)] bg-[var(--surface)]"><Image src={image.image_url} alt={image.alt_text??`${title} project image`} fill sizes={index===0?"(max-width: 1280px) 100vw, 1200px":"(max-width: 768px) 100vw, 600px"} className="object-cover sepia-[.25] contrast-[1.05]"/></div>{image.caption&&<figcaption className="mt-2 font-mono text-[.65rem] text-[var(--text-secondary)]">{image.caption}</figcaption>}</figure>)}</div></section>; }
