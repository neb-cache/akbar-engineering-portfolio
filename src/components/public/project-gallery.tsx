import Image from "next/image";
import type { PublicProjectImageRow } from "@/types/database";

export function ProjectGallery({ images, cover, title }: { images: PublicProjectImageRow[]; cover: string | null; title: string }) { const records:PublicProjectImageRow[]=cover?[{id:"cover",image_url:cover,alt_text:`${title} cover image`,caption:null,project_id:"",sort_order:-1,image_category:"interface"},...images.filter(image=>image.image_url!==cover)]:images; if(!records.length)return null; return <section aria-labelledby="project-gallery"><h2 id="project-gallery" className="editorial-label mb-5">Visual evidence</h2><div className="grid gap-4 md:grid-cols-2">{records.map((image,index)=><figure key={image.id} className={index===0?"md:col-span-2":""}><div className="relative aspect-[16/9] overflow-hidden border border-[var(--border)] bg-[var(--surface)]"><Image src={image.image_url} alt={image.alt_text??`${title} project image`} fill sizes={index===0?"(max-width: 1280px) 100vw, 1200px":"(max-width: 768px) 100vw, 600px"} className="object-cover sepia-[.25] contrast-[1.05]"/></div><figcaption className="mt-2 flex flex-wrap items-start justify-between gap-3 font-mono text-[.65rem] text-[var(--text-secondary)]"><span>{image.caption}</span>{image.image_category&&<span className="uppercase tracking-[.1em] text-[var(--accent-gold)]">{image.image_category}</span>}</figcaption></figure>)}</div></section>; }
