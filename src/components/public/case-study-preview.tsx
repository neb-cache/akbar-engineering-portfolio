import Link from "next/link";
import { ArrowUpRight } from "lucide-react";

export function CaseStudyPreview({ available }: { available: boolean }) {
  const concepts=["Five consuming platforms","Request validation","Consistent authentication","Rate limiting","Fallback strategy","Reduced ERP coupling"];
  return <section className="editorial-section"><div className="public-container"><div className="paper-panel grid overflow-hidden lg:grid-cols-[1.15fr_.85fr]"><div className="p-7 sm:p-12 lg:p-16"><p className="editorial-label !text-[var(--accent-brown)]">Featured case study / System dossier</p><h2 className="editorial-title mt-7 text-[clamp(3.25rem,9vw,5rem)]">Centralized Enterprise BFF</h2><p className="reading-measure mt-7 text-base leading-8 text-[rgba(24,19,15,.72)]">A critical ERP core needed protection from direct coupling across five consuming platforms. The BFF became an orchestration and standardization layer for validation, authentication, traffic controls, fallback behavior, and consistent contracts.</p><p className="mt-5 text-sm italic text-[var(--accent-brown)]">Selected implementation details are omitted due to confidentiality.</p>{available ? <Link href="/projects/centralized-enterprise-bff" className="button-base button-editorial mt-9 !border-[var(--ink)]">Read the public technical record <ArrowUpRight size={15}/></Link> : <p className="mt-9 font-mono text-xs uppercase tracking-[.1em] text-[var(--accent-brown)]">Apply Phase 2 migration to publish the sanitized record</p>}</div><aside className="technical-grid border-t border-[rgba(24,19,15,.25)] bg-[var(--paper-soft)] p-7 lg:border-l lg:border-t-0 lg:p-12"><p className="editorial-label !text-[var(--accent-brown)]">Architecture concerns</p><ol className="mt-8 divide-y divide-[rgba(24,19,15,.2)]">{concepts.map((concept,index)=><li key={concept} className="flex items-center gap-5 py-4"><span className="font-mono text-xs text-[var(--accent-brown)]">0{index+1}</span><span className="text-sm font-semibold">{concept}</span></li>)}</ol><p className="mt-8 border-t border-[var(--ink)] pt-5 text-sm leading-6">The architecture also accounts for the operational risk of introducing a central dependency and the need for resilient failure modes.</p></aside></div></div></section>;
}
